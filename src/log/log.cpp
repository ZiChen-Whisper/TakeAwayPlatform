// ============================================================
// 【本文件概述】
// 日志系统的占位实现文件
//
// 为什么这个文件几乎是空的？
// 因为 log.h 采用了 "Header-Only"（头文件专用）设计
// 所有代码（包括函数体 { }）都写在头文件里了
// 这个 .cpp 文件只是包含了头文件，确保编译系统能找到它
//
// Header-Only 的优点：
// - 使用简单，不需要单独编译 .cpp
// - 适合模板和简单工具类
//
// Header-Only 的缺点：
// - 头文件改动会导致所有包含它的文件重新编译
// - 不适合大型复杂类（编译时间会很长）
//
// 对于 Logger 这样的单例工具类，Header-Only 是很合适的选择
// ============================================================

#include "../include/log.h"  // 包含日志头文件（所有实现都在里面）

// 实现已在头文件中完成（Header-Only 设计）
// Logger 类的所有方法（getInstance、setLogFile、log 等）都直接在 log.h 中实现